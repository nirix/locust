{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "php": ">=5.4.0",
        "nirix/nanite": "dev-master"
    },
    "config": {
        "vendor-dir": "app/vendor/"
    }
}
